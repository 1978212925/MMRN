cfg = dict(
    model_name='',
    num_classes=2,
    lr_start=0.000005,
    weight_decay=5e-2,
    epochs=40,
    train_batch=6,
    val_batch=6,
    class_weights=[1, 1],
    respth='Running_Dict',
)
